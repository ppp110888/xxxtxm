# AI 伴学编程平台 — 项目计划书

> **项目代号：** CodeMate
> **版本：** v1.0.0
> **创建日期：** 2026-06-08
> **完成日期：** 2026-06-10
> **状态：** ✅ 三阶段开发全部完成

---

## 一、 系统定位与技术栈架构

CodeMate 是一个面向零基础学习者的自由学习式编程实训平台，专注 **Java** 与 **Python** 双语言教学。系统采用**单体前端 + 动态权限菜单**架构，底层依托高并发异步判题引擎与大模型 AI，提供沉浸式、有温度的代码伴学体验。所有学习内容自由访问，进度追踪但不设关卡限制。

### 1.1 前端基建

| 技术 | 版本 | 作用 |
|------|------|------|
| **Vue 3** + **Vite** | ≥3.4 / ≥5.0 | 前端框架与构建工具 |
| **Vue Router** | ≥4.3 | 动态路由管理，支撑管理端/学生端双端视图切换 |
| **Pinia** | ≥2.1 | 轻量级状态管理 |
| **Element Plus** | ≥2.5 | 桌面端 UI 组件库（中文 locale） |
| **Monaco Editor** | ≥0.45 | VS Code 内核的在线代码编辑器，含 Python/Java 代码片段自动补全 |

### 1.2 后端基建

| 技术 | 版本 | 作用 |
|------|------|------|
| **Spring Boot** | ≥3.2 | 主业务服务框架 |
| **MyBatis-Plus** | ≥3.5 | ORM 增强，含逻辑删除 + 自动填充 |
| **MySQL** | ≥8.0 | 关系型数据库，持久化存储 |
| **Sa-Token** | ≥1.37 | 轻量级 RBAC 权限框架，接口级鉴权 + 注解式角色校验 |

### 1.3 高并发与实时中间件

| 技术 | 版本 | 作用 |
|------|------|------|
| **Redis** | ≥7.0 | 知识树/题目缓存 + 会话管理 |
| **RabbitMQ** | ≥3.12 | 判题任务削峰解耦 |
| **WebSocket** | — | 判题结果全双工实时推送 |

### 1.4 核心判题引擎

**Go-Judge** — 独立部署的开源沙箱，基于 Linux cgroup 与 namespace 技术提供安全隔离的代码执行环境。开发环境使用本地 ProcessBuilder 执行。

### 1.5 AI 赋能层

第三方大模型 API（DeepSeek / 通义千问兼容协议） + **SSE (Server-Sent Events)** 流式通信，实现 AI 知识点讲解、代码纠错诊断、学情报告生成。

---

## 二、 核心业务模块

> 详细需求请参阅 [docs/requirements.md](./docs/requirements.md)

### 2.1 管理员/教师端（5 大模块）

| 模块 | 核心功能 | 技术实现 |
|------|---------|----------|
| **知识树管理** | 无限层级知识图谱 CRUD，Markdown 内容编辑 | Element Plus TreeTable + Markdown 编辑器 |
| **题库管理** | 题目元数据配置（难度/语言限制/时空限制） | 分页列表 + 多条件筛选 + 表单校验 |
| **测试用例车间** | `.in/.out` 文件上传 + 可见性分级（样本/隐藏） | Multipart 文件上传 + 磁盘存储 |
| **用户管理** | 学生账号管理、角色变更、昵称编辑 | 分页查询 + 软删除 |
| **题库-知识节点关联** | 例题/练习题绑定到知识节点 | 关联表 + 类型标记 |

### 2.2 学生端（5 大模块）

| 模块 | 核心功能 | 技术实现 |
|------|---------|----------|
| **技能树导航** | 双轨（Java/Python）知识图谱可视化，进度状态显示 | 卡片式布局 + 学习中/已完成状态标记 |
| **三阶学习舱** | 知识吸收 → 经典例题 → 实战练习 | 三阶步骤切换 + AI 知识点讲解面板 |
| **双栏在线 IDE** | Monaco Editor 编程 + 自测/提交双模式 + 实时判题反馈 | 语言切换 + WebSocket 推送 |
| **个人成长中心** | 数据画像 + 热力图 + 错题本 + 排行榜 + AI 学情报告 | 统计聚合 + CSS Grid 热力图 + SSE 流式 AI |
| **AI 代码纠错** | 提交错误后一键 AI 诊断，流式返回分析 | SSE EventSource + Markdown 渲染 |

---

## 三、 简历级技术亮点

| 面试考察维度 | 业务痛点 | 架构方案 |
|-------------|---------|---------|
| **RBAC 与接口安全** | 学生越权篡改题目/用例 | Sa-Token + `@SaCheckRole` 注解 + 动态路由 + Axios 拦截器 |
| **沙箱级安全隔离** | 死循环 / 内存溢出 / `os.system()` | Go-Judge + cgroup namespace + CPU 熔断 + 内存配额 |
| **高并发削峰解耦** | 晚高峰同步提交耗尽线程池 | RabbitMQ 异步化 + 判题消费集群平滑拉取 |
| **实时通信** | 判题异步化后反馈延迟 | WebSocket 全双工推送 + 心跳保活 + 指数退避重连 |
| **AI 流式诊断** | 报错信息难懂，初学者无从下手 | SSE 流式 + Prompt Engineering + 打字机效果渲染 |
| **多模式判题** | 开发/生产环境差异 | Profile 自适应：dev 模式本地执行 / prod 模式 Go-Judge |
| **自由学习模型** | 传统关卡制阻碍学习节奏 | 所有内容自由访问 + 进度追踪不设锁 + 双轨并行初始化 |

---

## 四、 敏捷迭代路线图（已全部完成）

```
阶段一（MVP）        阶段二（抗压进阶）        阶段三（AI 赋能）
  6-8 周               4-6 周                 3-4 周
 ─────────●────────────────────●──────────────────────●─────────►
          │                    │                      │
      2026-06-08           2026-06-09             2026-06-10
     RBAC + 数据录入      沙箱 + 高可用改造         AI 私教赋能
         ✅                   ✅                      ✅
```

### 阶段一：RBAC 基建与数据录入 ✅

1. ✅ 初始化 Spring Boot 3.2 + Vue 3 + Vite 工程
2. ✅ 集成 Sa-Token，实现登录/注册/角色验证/登出
3. ✅ 前端动态路由拦截器（管理端/学生端双视图切换）
4. ✅ 管理端：知识树 TreeTable CRUD + 题库 CRUD + 用例上传
5. ✅ 学生端：技能树进度展示 + 学习舱骨架
6. ✅ H2 开发数据库 + Docker Compose 生产中间件编排
7. ✅ 课程内容：Java + Python 双轨知识图谱（70+ Markdown 讲义）

### 阶段二：沙箱与高可用改造 ✅

1. ✅ 部署 Go-Judge 沙箱，编写 Java HTTP Client
2. ✅ 对接 Monaco Editor，开发 IDE 双栏页面（题目描述 + 代码编辑 + 输出面板）
3. ✅ 自测模式（不记录成绩）+ 正式提交模式
4. ✅ 引入 RabbitMQ 实现判题异步化（开发模式同步 fallback）
5. ✅ 开发 WebSocket 服务，实时推送判题结果
6. ✅ Redis 缓存知识树 + 题目数据（生产环境启用）
7. ✅ 个人成长中心：数据画像 + 热力图 + 错题本 + 3 个排行榜 + 通关列表
8. ✅ 判题 AC 自动标记知识节点为已完成（自由学习模式）

### 阶段三：AI 私教赋能 ✅

1. ✅ 对接 DeepSeek API（兼容 OpenAI 协议），编写 HTTP Client
2. ✅ AI 知识点讲解 — SSE 流式返回生活化类比讲解
3. ✅ AI 代码纠错诊断 — 提交错误后分析原因 + 修复建议
4. ✅ AI 学情报告 — 基于错题数据聚合生成周期学习分析
5. ✅ 前端 AI 诊断浮层面板（Markdown 渲染 + 打字机效果）
6. ✅ `useAiStream` 可组合函数（EventSource 封装 + 自动清理）

---

## 五、 系统架构总览

```
                          ┌──────────────────────────────────────────┐
                          │              前端 (Vue 3 + Vite)          │
                          │  ┌─────────┐  ┌──────────┐  ┌─────────┐ │
                          │  │管理端 SPA│  │ 学生端 SPA│  │Monaco IDE│ │
                          │  └────┬────┘  └────┬─────┘  └────┬────┘ │
                          └───────┼────────────┼─────────────┼──────┘
                                  │            │             │
                    HTTP/REST     │   SSE ◄────┼─── AI ─────┼── WebSocket
                                  ▼            ▼             ▼
                          ┌──────────────────────────────────────────┐
                          │          Spring Boot 3.2 后端              │
                          │  ┌────────┐ ┌──────────┐ ┌────────────┐ │
                          │  │Auth模块│ │Knowledge │ │Submission  │ │
                          │  │Sa-Token│ │  知识树   │ │  判题引擎   │ │
                          │  └────────┘ └──────────┘ └────────────┘ │
                          │  ┌────────┐ ┌──────────┐ ┌────────────┐ │
                          │  │Progress│ │ AI 模块   │ │ Growth中心  │ │
                          │  │学习进度│ │ SSE流式   │ │  数据统计   │ │
                          │  └────────┘ └──────────┘ └────────────┘ │
                          └──────┬───────┬──────────┬───────────────┘
                                 │       │          │
                      ┌──────────┘       │          └──────────────┐
                      ▼                  ▼                         ▼
               ┌──────────┐    ┌──────────────┐          ┌──────────────┐
               │  MySQL   │    │ Redis + 缓存  │          │   Go-Judge   │
               │  持久化   │    │  + RabbitMQ   │          │   安全沙箱    │
               └──────────┘    └──────────────┘          └──────────────┘
```

---

## 六、 数据库核心表

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `t_user` | 用户表 | id, username, password(BCrypt), role(admin/student), nickname |
| `t_knowledge_node` | 知识节点表 | id, parent_id, name, markdown_content, sort_order |
| `t_question` | 题目表 | id, title, difficulty, language_limit, time/memory限制, description |
| `t_test_case` | 测试用例表 | id, question_id, input/output_file_path, is_visible |
| `t_question_knowledge` | 题目-知识关联表 | id, question_id, knowledge_id, type(example/practice) |
| `t_submission` | 提交记录表 | id, user_id, question_id, code, status, time_used, memory_used |
| `t_user_progress` | 学习进度表 | id, user_id, knowledge_id, status(in_progress/cleared) |

---

## 七、 完整 API 端点一览（39 个端点）

### 7.1 认证接口（4 个，公开）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/auth/register` | 用户注册 |
| POST | `/api/v1/auth/login` | 用户登录 → 返回 Sa-Token |
| GET | `/api/v1/auth/me` | 获取当前用户信息 |
| POST | `/api/v1/auth/logout` | 登出 |

### 7.2 管理端接口（16 个，admin 角色）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/v1/admin/knowledge` | 知识树列表 / 新增节点 |
| PUT/DELETE | `/api/v1/admin/knowledge/{id}` | 更新 / 删除节点（级联子节点） |
| GET/POST | `/api/v1/admin/questions` | 题目列表 / 新增题目 |
| PUT/DELETE | `/api/v1/admin/questions/{id}` | 更新 / 删除题目 |
| GET/POST | `/api/v1/admin/cases` | 测试用例列表 / 上传用例 |
| DELETE | `/api/v1/admin/cases/{id}` | 删除测试用例 |
| GET/PUT/DELETE | `/api/v1/admin/users` | 用户列表 / 编辑角色昵称 / 删除 |

### 7.3 学生端接口（19 个，student 角色）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/student/knowledge/tree` | 获取知识树（含进度） |
| GET | `/api/v1/student/knowledge/{id}` | 获取节点详情（Markdown 讲义） |
| GET | `/api/v1/student/questions` | 题目列表（支持知识节点/类型筛选） |
| GET | `/api/v1/student/questions/{id}` | 题目详情 + 样本用例预览 |
| POST | `/api/v1/student/submit` | 🚀 提交代码（判题） |
| POST | `/api/v1/student/run` | 🔧 自测运行（不记录成绩） |
| GET | `/api/v1/student/submissions` | 我的提交列表 |
| GET | `/api/v1/student/submissions/{id}` | 提交详情 |
| GET | `/api/v1/student/submissions/best-status` | 批量查询最佳状态 |
| GET | `/api/v1/student/progress` | 我的学习进度 |
| GET | `/api/v1/student/growth/stats` | 📊 个人统计数据 |
| GET | `/api/v1/student/growth/wrong-answers` | 📝 错题列表 |
| GET | `/api/v1/student/growth/heatmap` | 🔥 提交热力图 |
| GET | `/api/v1/student/growth/leaderboard/*` | 🏆 3 个排行榜 |
| GET | `/api/v1/student/growth/cleared-questions` | ✅ 通关题目列表 |
| GET | `/api/v1/student/ai/explain/knowledge/{id}` | 🤖 AI 知识点讲解 (SSE) |
| GET | `/api/v1/student/ai/report` | 📋 AI 学情报告 (SSE) |
| GET | `/api/v1/student/ai/diagnose/{submissionId}` | 🔍 AI 代码诊断 (SSE) |

---

## 八、 项目目录结构

```
xxxtxm/
├── CLAUDE.md                       # 项目开发指引
├── 计划书.md                        # 本文件 — 项目总览
├── docker-compose.yml              # 生产中间件编排（MySQL/Redis/RabbitMQ/Go-Judge）
├── docs/                            # 标准规范文档
│   ├── architecture.md              # 系统架构设计
│   ├── requirements.md              # 需求规格说明
│   ├── tech-spec.md                 # 技术规范
│   ├── design-spec.md               # UI/UX 设计规范
│   └── execution-plan.md            # 分阶段执行步骤
├── dev-diary/                       # 开发日志（按日期命名）
├── backend/                         # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/xxxtxm/
│       ├── CodeMateApplication.java
│       ├── common/                  # 通用模块（结果封装、异常处理、配置）
│       │   ├── config/              # SaToken、MyBatisPlus、缓存、MQ、AI、数据初始化
│       │   ├── exception/           # 全局异常处理 + 业务异常
│       │   └── result/              # 统一响应 R<T>
│       └── modules/
│           ├── auth/                # 认证模块（User CRUD、登录/注册）
│           ├── knowledge/           # 知识树模块（树结构 CRUD）
│           ├── question/            # 题库模块（题目 + 测试用例 + 关联）
│           ├── submission/          # 判题模块（提交 + 沙箱执行 + WebSocket）
│           ├── progress/            # 进度模块（学习进度 + 成长中心）
│           └── ai/                  # AI 模块（模型客户端 + 流式服务）
├── frontend/                        # Vue 3 前端
│   ├── package.json
│   └── src/
│       ├── main.js                  # 入口（Pinia + ElementPlus + Router + Monaco）
│       ├── App.vue
│       ├── router/                  # 路由配置 + 权限守卫
│       ├── layouts/                 # AdminLayout（侧边栏）+ StudentLayout（顶栏）
│       ├── views/                   # 页面视图
│       │   ├── Login.vue
│       │   ├── admin/               # KnowledgeTree, QuestionList, CaseUpload, UserList
│       │   └── student/             # SkillTree, LearningCabin, IdeWorkspace, GrowthCenter
│       ├── components/              # MonacoEditor 封装
│       ├── composables/             # useAsyncData, useJudgeSocket, useAiStream
│       ├── stores/                  # Pinia user store
│       ├── api/                     # 6 个 API 模块
│       ├── utils/                   # request 封装, constants, markdown 渲染
│       └── styles/                  # 全局 SCSS 变量
└── sandbox/                         # Go-Judge 沙箱配置
```

---

## 九、 快速启动

```bash
# 零 Docker 开发模式（推荐）
cd backend && mvn spring-boot:run -Dspring-boot.run.profiles=dev
cd frontend && pnpm dev

# Docker 全栈模式
docker compose up -d                    # 启动 MySQL/Redis/RabbitMQ/Go-Judge
cd backend && mvn spring-boot:run      # 后端连接 Docker 中间件
cd frontend && pnpm dev                # 前端

# 服务端口
# 后端 API:  http://localhost:8080
# 前端 Dev:  http://localhost:5173
# H2 Console: http://localhost:8080/h2-console (dev 模式)
# MySQL:     localhost:3306  root/root123
# RabbitMQ:  http://localhost:15672 guest/guest
# Go-Judge:  http://localhost:5050
```

---

## 十、 文档索引

| 文档 | 路径 | 说明 |
|------|------|------|
| **架构设计** | [docs/architecture.md](./docs/architecture.md) | 分层架构图、核心流程图、模块划分、数据库设计、部署拓扑 |
| **需求规格** | [docs/requirements.md](./docs/requirements.md) | 功能需求清单、非功能需求、用户故事、验收标准 |
| **技术规范** | [docs/tech-spec.md](./docs/tech-spec.md) | 技术栈版本、代码规范、API 设计规范、数据库规范、安全规范 |
| **设计规范** | [docs/design-spec.md](./docs/design-spec.md) | UI/UX 设计原则、布局规范、组件规范、响应式策略 |
| **执行步骤** | [docs/execution-plan.md](./docs/execution-plan.md) | 分阶段任务拆解、工时预估、里程碑、风险预案 |
| **开发日志** | [dev-diary/](./dev-diary/) | 每日开发记录 |
| **项目指引** | [CLAUDE.md](./CLAUDE.md) | 工作约定、文档索引、技术速查、项目结构 |

---

> **最后更新：** 2026-06-10
> **当前状态：** ✅ 三阶段开发全部完成 — 系统可正常运行
